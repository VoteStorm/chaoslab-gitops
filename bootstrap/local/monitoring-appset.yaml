apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: monitoring
  namespace: argocd
spec:
  goTemplate: true
  goTemplateOptions: ["missingkey=error"]
  generators:
    - git:
        repoURL: https://github.com/VoteStorm/chaoslab-gitops.git
        revision: feature/on-prem/#14-alerting-and-ingress
        files:
          - path: apps/monitoring/*/app.yaml
  template:
    metadata:
      name: "{{ .name }}"
    spec:
      project: default
      ignoreDifferences:
        - group: apps
          kind: StatefulSet
          jsonPointers:
            - /spec/volumeClaimTemplates/0/apiVersion
            - /spec/volumeClaimTemplates/0/kind
      sources:
        - repoURL: https://github.com/VoteStorm/chaoslab-gitops.git
          targetRevision: feature/on-prem/#10-podinfo-chaos-mesh
          ref: values
        - repoURL: "{{ .repoURL }}"
          chart: "{{ .chart }}"
          targetRevision: "{{ .targetRevision }}"
          helm:
            ignoreMissingValueFiles: true
            valueFiles:
              - $values/apps/monitoring/{{ .name }}/values.yaml         # 공통
              - $values/apps/monitoring/{{ .name }}/dashboards.yaml
              - $values/apps/monitoring/{{ .name }}/rules.yaml
              - $values/apps/monitoring/{{ .name }}/local/values.yaml   # 환경별
      destination:
        server: https://kubernetes.default.svc
        namespace: "{{ .namespace }}"
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
          - ServerSideApply=true
